import { StyleSheet } from "react-native";

export const sharedStyles = StyleSheet.create({
  infoItem: {
    alignItems: "center",
    borderRadius: 16,
    paddingVertical: 8,
    paddingHorizontal: 16,
  },
});
